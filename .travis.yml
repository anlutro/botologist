branches: [master]

language: python

python:
  - '3.6'
  - '3.7'
  - '3.8'

install:
  - pip install -r requirements/dev.txt

script:
  - pytest
  - pylint -E botologist plugins

sudo: false

cache: pip
